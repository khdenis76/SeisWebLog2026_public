# Baseproject SeisWebLog UI gap fix

Built from the original full UI archive. All Python files are preserved.

Changes:
- Removed `h-100` from left/right main panels so they do not stretch the page.
- Changed main grid alignment to `flex-start`.
- Reduced map container height and point table height.
- Added CSS overrides only in `baseproject/static/baseproject/css/baseproject_seisweblog.css`.
